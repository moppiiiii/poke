pre-commit:
  parallel: false
  commands:
    audit:
      run: pnpm audit
    eslint:fix:
      run: pnpm lint:fix && git add {staged_files}
    prettier:fix:
      run: pnpm format:fix && git add {staged_files}
